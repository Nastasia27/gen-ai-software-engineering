# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Pipeline runtime artifacts — keep the dirs (.gitkeep), ignore the messages
shared/input/*.json
shared/processing/*.json
shared/output/*.json
shared/results/*.json

# OS
.DS_Store
